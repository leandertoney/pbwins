[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Increase timeout for DUPR scraping operations
  timeout = 60

# Scheduled function runs automatically every hour
# No additional configuration needed - Netlify detects it from the code
